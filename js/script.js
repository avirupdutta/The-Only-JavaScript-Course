// Topic -  Control Flow in JS
/**
 * 1) if else
 * 2) switch case
 * 3) Ternary operator
 */

var age = 9;

console.log(age >= 18 ? (
	age <= 10 ? 'age is less than 10': 'age is more than 10'
	) : "You are not an adult");
